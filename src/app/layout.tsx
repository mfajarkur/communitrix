import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-screen flex justify-center items-start overflow-y-auto bg-[#f3f4f6]">
        <div className="w-full max-w-[480px] min-h-screen bg-white text-[#111827] border-x border-gray-100 shadow-[0_4px_30px_rgba(0,0,0,0.03)] flex flex-col relative">
          {children}
        </div>
      </body>
    </html>
  );
}
